@import 'mixins';

.about { width: 460px; }

.admin .sign-out {
  width: 620px;
  margin: 0 auto;
  padding-left: 40px 
}

.contact {
  position: relative;

  img {
    float: left;
    margin-top: -100px;
    margin-left: 115px;
  }

  p { padding-right: 35px; }
}
